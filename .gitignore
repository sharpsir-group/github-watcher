# Environment (secrets, API keys, credentials)
.env
.env.*
!.env.example

# Legacy (pre-v1.2) — remove once all instances migrated
.secrets
.aws-credentials

# Instance-specific config (use config.example.json as template)
config.json

# Deployment logs
logs/

# Node
node_modules/
